# To Reinstall Dependencies After Pulling:
1. Navigate to the parent folder containing the Django project, virtual environment and app.
2. Activate the virtual environment by running [Your venv name]\Scripts\activate.bat" in your terminal.
3. Run the command "pip install -r requirements.txt".
